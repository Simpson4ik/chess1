<Window x:Class="ChessUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChessUI"
        mc:Ignorable="d"
        Title="Chess" 
        Width="980" Height="700" MinWidth="900" MinHeight="700" 
        Icon="Assets/icon.ico"
        WindowStartupLocation="CenterScreen"
        Background="Black"
        KeyDown="Window_KeyDown"
        WindowStyle="None" AllowsTransparency="True" ResizeMode="NoResize">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="10,10,5,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <ItemsControl Grid.Row="0" Grid.Column="0" Margin="0,0,8,0" VerticalAlignment="Stretch">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Rows="8" Columns="1"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Foreground="{StaticResource TextColor}" FontFamily="Arial" FontSize="11" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" MinHeight="15"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <System:String xmlns:System="clr-namespace:System;assembly=mscorlib">8</System:String>
                <System:String xmlns:System="clr-namespace:System;assembly=mscorlib">7</System:String>
                <System:String xmlns:System="clr-namespace:System;assembly=mscorlib">6</System:String>
                <System:String xmlns:System="clr-namespace:System;assembly=mscorlib">5</System:String>
                <System:String xmlns:System="clr-namespace:System;assembly=mscorlib">4</System:String>
                <System:String xmlns:System="clr-namespace:System;assembly=mscorlib">3</System:String>
                <System:String xmlns:System="clr-namespace:System;assembly=mscorlib">2</System:String>
                <System:String xmlns:System="clr-namespace:System;assembly=mscorlib">1</System:String>
            </ItemsControl>
            <ItemsControl Grid.Row="1" Grid.Column="1" Margin="0,8,0,0" HorizontalAlignment="Stretch">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Rows="1" Columns="8"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Foreground="{StaticResource TextColor}" FontFamily="Arial" FontSize="12" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" MinWidth="15"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <System:String xmlns:System="clr-namespace:System;assembly=mscorlib">a</System:String>
                <System:String xmlns:System="clr-namespace:System;assembly=mscorlib">b</System:String>
                <System:String xmlns:System="clr-namespace:System;assembly=mscorlib">c</System:String>
                <System:String xmlns:System="clr-namespace:System;assembly=mscorlib">d</System:String>
                <System:String xmlns:System="clr-namespace:System;assembly=mscorlib">e</System:String>
                <System:String xmlns:System="clr-namespace:System;assembly=mscorlib">f</System:String>
                <System:String xmlns:System="clr-namespace:System;assembly=mscorlib">g</System:String>
                <System:String xmlns:System="clr-namespace:System;assembly=mscorlib">h</System:String>
            </ItemsControl>
            <Viewbox Grid.Row="0" Grid.Column="1" Stretch="Uniform">
                <Grid x:Name="BoardGrid" Width="600" Height="600" MouseDown="BoardGrid_MouseDown">
                    <Grid.Background>
                        <ImageBrush ImageSource="Assets/Board.png" />
                    </Grid.Background>
                    <UniformGrid x:Name="HighlightGrid" Rows="8" Columns="8"/>
                    <UniformGrid x:Name="PieceGrid" Rows="8" Columns="8" RenderOptions.BitmapScalingMode="HighQuality"/>
                    <ContentControl x:Name="MenuContainer"/>
                </Grid>
            </Viewbox>
        </Grid>

        <Border Grid.Column="1" Style="{StaticResource BorderStyle}" Background="#FF2c0202" Margin="0,10,10,10" Padding="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock x:Name="BlackTimerText" Grid.Row="0" Text="03:00" Style="{StaticResource TextStyle}" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center" Margin="5"/>
                <WrapPanel x:Name="BlackCapturedPanel" Grid.Row="1" MinHeight="35" Margin="0,5,0,10"/>
                <TextBlock Text="ІСТОРІЯ ХОДІВ" Grid.Row="2" Style="{StaticResource TextStyle}" FontSize="16" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,10,0,5"/>
                <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto" Margin="0,0,0,10">
                    <ListBox x:Name="MoveHistoryDisplay" 
                             Background="Transparent" 
                             BorderThickness="0" 
                             Foreground="{StaticResource TextColor}" 
                             FontFamily="Segoe UI Light" 
                             FontSize="13">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="3,2"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <ContentPresenter/>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>
                </ScrollViewer>
                <WrapPanel x:Name="WhiteCapturedPanel" Grid.Row="4" MinHeight="35" Margin="0,10,0,5"/>
                <TextBlock x:Name="WhiteTimerText" Grid.Row="5" Text="03:00" Style="{StaticResource TextStyle}" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center" Margin="5"/>
            </Grid>
        </Border>
    </Grid>
</Window>